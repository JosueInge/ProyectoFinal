<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectofinal1">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-feature android:name="android.hardware.camera" android:required="true"/>

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinal1">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".MiradoresActivity" />
        <activity android:name=".PlayasActivity" />
        <activity android:name=".PlazasActivity" />
        <activity android:name=".BalneariosActivity" />
        <activity android:name=".CaminatasActivity" />
        <activity android:name=".CinesActivity" />
        <activity android:name=".ParquesActivity" />
        <activity android:name=".RuinasActivity" />
        <activity android:name=".HotelesActivity" />
        <activity android:name=".RestaurantesActivity" />
        <activity android:name=".ContratoActivity" />
        <activity android:name=".lista_contrato" />
    </application>

</manifest>